---
phase: 07-polish-accessibility
plan: 10
type: execute
wave: 4
depends_on: [07-01, 07-04, 07-05, 07-06, 07-09]
files_modified:
  - PhotoEditor/ContentView.swift
  - PhotoEditor/Library/LibraryGridView.swift
  - PhotoEditor/Library/LibraryItemThumbnail.swift
  - PhotoEditor/Library/RecipesSheetView.swift
  - PhotoEditor/Export/ExportSheetView.swift
autonomous: true
requirements: [UX-01, UX-07, UX-09]
must_haves:
  truths:
    - "ContentView background, editor preview placeholder, library/recipes empty states, and export sheet all use Theme.Colors tokens"
    - "No remaining Color(.systemGroupedBackground), Color(.tertiarySystemBackground), Color(.secondarySystemBackground) in app surfaces — all replaced with Theme.Colors.canvas / .panel"
    - "iPhone+iPad layout audit: all sheets use .presentationDetents that don't crash on iPad regular size class; no fixed widths >320pt that clip on iPhone SE"
    - "App-wide accentColor set via .tint(Theme.Colors.accent) at the NavigationStack root"
  artifacts:
    - path: "PhotoEditor/ContentView.swift"
      provides: "Themed root navigation + app-wide tint"
    - path: "PhotoEditor/Library/LibraryGridView.swift"
      provides: "Themed library grid + empty state"
    - path: "PhotoEditor/Library/LibraryItemThumbnail.swift"
      provides: "Themed thumbnail placeholder"
    - path: "PhotoEditor/Library/RecipesSheetView.swift"
      provides: "Themed recipes sheet + empty state"
    - path: "PhotoEditor/Export/ExportSheetView.swift"
      provides: "Themed export sheet"
  key_links:
    - from: "NavigationStack root in ContentView"
      to: "Theme.Colors.accent applied via .tint"
      via: ".tint(Theme.Colors.accent)"
      pattern: "\\.tint\\(Theme\\.Colors\\.accent\\)"
---

<objective>
Final theme application sweep — bring the remaining surfaces (root navigation, library grid, recipes sheet, export sheet, thumbnail placeholders) onto Theme tokens. Apply app-wide tint at NavigationStack root so any incidental SwiftUI control (toolbar buttons, ProgressView) inherits Theme accent. Audit for iPad layout breakage.

Purpose: UX-01 (distinctive identity end-to-end), UX-07 (deliberate light/dark surfaces), UX-09 (iPad runs without crashing/clipping).
Output: Themed remaining views.
</objective>

<execution_context>
@/home/matt/.claude/get-shit-done/workflows/execute-plan.md
@/home/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-polish-accessibility/07-CONTEXT.md
@PhotoEditor/ContentView.swift
@PhotoEditor/Library/LibraryGridView.swift
@PhotoEditor/Library/RecipesSheetView.swift
@PhotoEditor/Design/Theme.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply Theme to ContentView root and library/recipes/export surfaces</name>
  <files>PhotoEditor/ContentView.swift, PhotoEditor/Library/LibraryGridView.swift, PhotoEditor/Library/LibraryItemThumbnail.swift, PhotoEditor/Library/RecipesSheetView.swift, PhotoEditor/Export/ExportSheetView.swift</files>
  <action>
**ContentView.swift:**
1. Replace `.background(Color(.systemGroupedBackground))` → `.background(Theme.Colors.canvas)`.
2. After `.navigationBarTitleDisplayMode(.inline)`, add: `.tint(Theme.Colors.accent)` on the NavigationStack so toolbar buttons (PhotosPicker, Library, Recipes, Export, Save buttons) all inherit Theme accent.
3. `editorPreview` placeholder: replace `Color(.tertiarySystemBackground)` → `Theme.Colors.panel`.
4. Empty state: replace `.foregroundStyle(.blue)` on the photo.badge.plus icon → `Theme.Colors.accent`.
5. Empty state Text: ensure font uses Theme.Typography.subtitle.
6. Sheet `.presentationDetents([.medium, .large])` is fine on iPad (system handles regular size class). Do not change.

**LibraryGridView.swift:**
1. Empty state icon `.foregroundStyle(.secondary)` → `Theme.Colors.secondary`.
2. Empty state Text fonts → Theme.Typography.subtitle / .body.
3. NavigationStack background: add `.background(Theme.Colors.canvas.ignoresSafeArea())` outside the Group.

**LibraryItemThumbnail.swift:**
1. `Color(.tertiarySystemBackground)` → `Theme.Colors.panel`.
2. Any Text fonts → Theme.Typography (covered by 07-09 sweep but verify).

**RecipesSheetView.swift:**
1. Empty state Image `.foregroundStyle(.purple)` → `Theme.Colors.accent`.
2. Empty state Text fonts → Theme.Typography.subtitle / .body.
3. RecipeRow thumbnail LinearGradient — keep visual richness but use brand colors:
   ```swift
   LinearGradient(
       colors: [Theme.Colors.accent.opacity(0.7), Theme.Colors.accent.opacity(0.3)],
       startPoint: .topLeading, endPoint: .bottomTrailing
   )
   ```

**ExportSheetView.swift:**
1. Read the file first to identify hard-coded colors / fonts.
2. Replace any `Color.blue`, `Color.accentColor`, `Color(.system*)` with appropriate Theme.Colors token.
3. Replace `.font(.headline | .subheadline | .body | .caption)` with Theme.Typography equivalents.
4. Quality slider tint should be `Theme.Colors.accent`.

**iPad audit (UX-09):**
- Search for any `.frame(width: <fixed pt value>)` greater than 320 in the swept files. If found, replace with `.frame(maxWidth: <pt value>)` or remove entirely so the layout adapts to wider iPad screens.
- Search for `.fixedSize(horizontal: true, vertical: false)` on labels — these can clip at large Dynamic Type. Note in summary if any are found; only change if clearly problematic.
- Sheets already use `.presentationDetents` — confirmed safe on iPad as of iOS 17.
  </action>
  <verify>
    <automated>grep -q "Theme.Colors.canvas" PhotoEditor/ContentView.swift && grep -q "\.tint(Theme.Colors.accent)" PhotoEditor/ContentView.swift && ! grep -q "Color(\.systemGroupedBackground)\|Color(\.tertiarySystemBackground)" PhotoEditor/ContentView.swift && grep -q "Theme.Colors" PhotoEditor/Library/LibraryGridView.swift && grep -q "Theme.Colors" PhotoEditor/Library/RecipesSheetView.swift && grep -q "Theme.Colors" PhotoEditor/Library/LibraryItemThumbnail.swift && grep -q "Theme.Colors" PhotoEditor/Export/ExportSheetView.swift</automated>
  </verify>
  <done>All five surfaces use Theme.Colors tokens; ContentView applies app-wide tint via Theme.Colors.accent; no remaining systemX background references in these files.</done>
</task>

</tasks>

<verification>
- grep finds zero `Color(.systemGroupedBackground|.tertiarySystemBackground|.secondarySystemBackground)` in the swept files
- `.tint(Theme.Colors.accent)` is present on the NavigationStack
- iPhone SE (320pt) and iPad regular size class both render without clipping
</verification>

<success_criteria>
The app is end-to-end themed: light + dark mode show deliberate brand colors, accent (warm amber) is consistent, no Apple-default surface tones remain in user-facing chrome. UX-01, UX-07, UX-09 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-accessibility/07-10-SUMMARY.md`
</output>
