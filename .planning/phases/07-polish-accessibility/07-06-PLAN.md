---
phase: 07-polish-accessibility
plan: 06
type: execute
wave: 2
depends_on: [07-01, 07-02, 07-03]
files_modified:
  - PhotoEditor/Editor/Panels/PanelContainerView.swift
  - PhotoEditor/Editor/Panels/UndoToolbar.swift
autonomous: true
requirements: [UX-02, UX-03, UX-06]
must_haves:
  truths:
    - "Panel tab switch animation uses Motion.adaptive(.panel) (was hard-coded .spring)"
    - "Tapping a tab fires Haptic.panelOpen"
    - "Undo and Redo buttons fire Haptic.undoRedo on tap"
    - "Panel background uses Theme.Colors.panel; tab icons use Theme.Colors.accent when selected"
  artifacts:
    - path: "PhotoEditor/Editor/Panels/PanelContainerView.swift"
      provides: "Themed panel container with adaptive spring transitions and tab haptics"
    - path: "PhotoEditor/Editor/Panels/UndoToolbar.swift"
      provides: "Themed undo toolbar with undoRedo haptics"
  key_links:
    - from: "Tab button action"
      to: "withAnimation(Motion.adaptive(Motion.panel)) + Haptic.play(.panelOpen)"
      via: "tabBar Button action"
      pattern: "Motion\\.adaptive"
---

<objective>
Polish the panel container (panel surface + tab bar) and the undo toolbar. Both are persistent surfaces visible during the entire editing session — they set the chrome tone of the app.

Purpose: UX-02 (panel/undo haptics), UX-03 (spring transitions, no canvas shift), UX-06 (Reduce Motion compliance via Motion.adaptive).
Output: Updated PanelContainerView + UndoToolbar consuming Theme + Haptic + Motion.
</objective>

<execution_context>
@/home/matt/.claude/get-shit-done/workflows/execute-plan.md
@/home/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-polish-accessibility/07-CONTEXT.md
@PhotoEditor/Editor/Panels/PanelContainerView.swift
@PhotoEditor/Editor/Panels/UndoToolbar.swift
@PhotoEditor/Design/Theme.swift
@PhotoEditor/Design/Haptics.swift
@PhotoEditor/Design/Motion.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply Theme + Motion + Haptics to PanelContainerView</name>
  <files>PhotoEditor/Editor/Panels/PanelContainerView.swift</files>
  <action>
Edit `PhotoEditor/Editor/Panels/PanelContainerView.swift`:

1. Panel background: replace `Color(.secondarySystemBackground)` with `Theme.Colors.panel`.
2. Panel corner radius: replace `cornerRadius: 20` with `cornerRadius: Theme.Radii.large`.
3. Tab bar button: replace hard-coded `withAnimation(.spring(duration: 0.3, bounce: 0.2))` with `withAnimation(Motion.adaptive(Motion.panel))`.
4. In the same Button action, ALSO call `Haptic.play(.panelOpen)` (only when `selectedTab != tab`):
   ```swift
   Button {
       guard selectedTab != tab else { return }
       Haptic.play(.panelOpen)
       withAnimation(Motion.adaptive(Motion.panel)) {
           selectedTab = tab
       }
   } label: { ... }
   ```
5. Tab icon font: keep `.font(.system(size: 18, weight: .semibold))` ONLY if needed for icon sizing (SF Symbol size). This is acceptable — icons are not text. Leave the line as-is.
6. Tab title text: replace `.font(.caption2)` with `.font(Theme.Typography.caption)`.
7. Tab foregroundStyle: replace `Color.accentColor` with `Theme.Colors.accent`, and `.secondary` with `Theme.Colors.secondary`.
8. Preserve existing `.accessibilityLabel`, `.accessibilityAddTraits` (UX-05).
  </action>
  <verify>
    <automated>grep -q "Theme.Colors.panel" PhotoEditor/Editor/Panels/PanelContainerView.swift && grep -q "Theme.Colors.accent" PhotoEditor/Editor/Panels/PanelContainerView.swift && grep -q "Motion.adaptive(Motion.panel)" PhotoEditor/Editor/Panels/PanelContainerView.swift && grep -q "Haptic.play(.panelOpen)" PhotoEditor/Editor/Panels/PanelContainerView.swift && grep -q "Theme.Radii.large" PhotoEditor/Editor/Panels/PanelContainerView.swift && grep -q "accessibilityLabel" PhotoEditor/Editor/Panels/PanelContainerView.swift</automated>
  </verify>
  <done>PanelContainerView consumes Theme/Motion/Haptic; tab transition uses adaptive spring; selecting same tab is a no-op (no spurious haptic).</done>
</task>

<task type="auto">
  <name>Task 2: Apply Theme + Haptics to UndoToolbar</name>
  <files>PhotoEditor/Editor/Panels/UndoToolbar.swift</files>
  <action>
Edit `PhotoEditor/Editor/Panels/UndoToolbar.swift`:

1. Undo button action: prepend `Haptic.play(.undoRedo)` before `viewModel.undo()`.
2. Redo button action: prepend `Haptic.play(.undoRedo)` before `viewModel.redo()`.
3. Reset All button action (the "Reset" inside the alert): prepend `Haptic.play(.recipeApply)` (success — discrete confirmed action).
4. Reset All Label font: replace `.font(.subheadline.weight(.semibold))` with `.font(Theme.Typography.subtitle)`.
5. The two icon-only buttons keep `.font(.system(size: 18, weight: .semibold))` — that is SF Symbol sizing, not text styling. Leave as-is.
6. Disabled state opacity is handled by `.disabled()` modifier already (system default ~0.4 for disabled). No change needed.
7. Preserve all `.accessibilityLabel(...)` calls (UX-05).
8. Note: do NOT add haptics inside the `.alert` itself for showing/dismissing — only on the destructive action confirmation.
  </action>
  <verify>
    <automated>grep -c "Haptic.play(.undoRedo)" PhotoEditor/Editor/Panels/UndoToolbar.swift | awk '$1>=2{exit 0}{exit 1}' && grep -q "Haptic.play(.recipeApply)" PhotoEditor/Editor/Panels/UndoToolbar.swift && grep -q "Theme.Typography.subtitle" PhotoEditor/Editor/Panels/UndoToolbar.swift && grep -q "accessibilityLabel" PhotoEditor/Editor/Panels/UndoToolbar.swift</automated>
  </verify>
  <done>Undo and Redo each play undoRedo haptic; Reset All confirmation plays recipeApply (success). Reset All label uses Theme typography.</done>
</task>

</tasks>

<verification>
- Tab tap fires haptic only on actual change
- Undo/Redo always fire haptic (button is already disabled when not actionable)
- Reduce Motion compliance via Motion.adaptive on the spring
</verification>

<success_criteria>
Editor chrome (panel + tab bar + undo toolbar) feels coherent, tactile, and on-brand. Reduce Motion testing simply requires toggling iOS Settings; animations bypass automatically.
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-accessibility/07-06-SUMMARY.md`
</output>
