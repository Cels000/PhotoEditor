---
phase: 05-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - PhotoEditor/Export/ExportOptions.swift
autonomous: true
requirements: [EXPORT-03, EXPORT-04, EXPORT-05]
must_haves:
  truths:
    - "ExportFormat exposes JPEG, HEIC, PNG cases with UTI mapping"
    - "ExportSize exposes full / web (2048) / story (1080) / custom presets"
    - "Custom long-edge values are clamped to 256...8192"
    - "ExportOptions is Codable and has a sensible default (HEIC, full, 0.85)"
    - "PNG path does not expose a quality slider"
  artifacts:
    - path: "PhotoEditor/Export/ExportOptions.swift"
      provides: "ExportFormat / ExportSize / ExportOptions value types"
      contains: "enum ExportFormat"
  key_links:
    - from: "ExportFormat"
      to: "UTType identifiers"
      via: "var uti: String"
      pattern: "UTType\\.(jpeg|heic|png)\\.identifier"
    - from: "ExportSize.custom"
      to: "clamp 256...8192"
      via: "init / resolve(sourceLongEdge:)"
      pattern: "256\\.\\.\\.8192|max\\(256.*min\\(8192"
---

<objective>
Define the value-type contracts that every other Phase 5 plan consumes: `ExportFormat`, `ExportSize`, and `ExportOptions`. These are pure Foundation/UTType types with no Core Image, no UIKit — the canonical export configuration model.

Purpose: Downstream plans (encoder, save-to-Photos, share-sheet, view-model wiring, UI) all import these types. Defining contracts first means executors implement against them rather than discovering them.
Output: One Swift file containing the three exported types plus a `resolve(sourceLongEdge:)` helper on `ExportSize`.
</objective>

<execution_context>
@/home/matt/.claude/get-shit-done/workflows/execute-plan.md
@/home/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-export/05-CONTEXT.md

<interfaces>
<!-- This plan CREATES the contracts. No upstream interfaces to import. -->
<!-- UTType is from UniformTypeIdentifiers framework — system-provided. -->

System types used:
```swift
import UniformTypeIdentifiers
UTType.jpeg.identifier   // "public.jpeg"
UTType.heic.identifier   // "public.heic"
UTType.png.identifier    // "public.png"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define ExportFormat / ExportSize / ExportOptions value types</name>
  <files>PhotoEditor/Export/ExportOptions.swift</files>
  <behavior>
    - ExportFormat: enum with cases .jpeg, .heic, .png; Codable; Hashable; CaseIterable
      - var uti: String returns UTType.{jpeg|heic|png}.identifier
      - var fileExtension: String returns "jpg" / "heic" / "png"
      - var supportsQuality: Bool — true for .jpeg/.heic, false for .png
      - var displayName: String — "JPEG" / "HEIC" / "PNG"
    - ExportSize: enum with cases .full, .web, .story, .custom(longEdge: Int); Codable; Hashable
      - func resolve(sourceLongEdge: Int) -> Int
        - .full → sourceLongEdge
        - .web → 2048
        - .story → 1080
        - .custom(n) → max(256, min(8192, n))
      - var displayName: String — "Full" / "Web (2048)" / "Story (1080)" / "Custom"
    - ExportOptions: struct, Codable, Equatable
      - format: ExportFormat (default .heic)
      - size: ExportSize (default .full)
      - quality: Double (default 0.85; only meaningful when format.supportsQuality)
      - static let `default` = ExportOptions()
    - Custom-size clamp boundary: ExportSize.custom(100).resolve(sourceLongEdge: 9999) == 256;
      ExportSize.custom(99999).resolve(sourceLongEdge: 100) == 8192.
    - Round-trip: JSONEncoder().encode then JSONDecoder().decode preserves all fields including .custom associated value.
  </behavior>
  <action>
    Create `PhotoEditor/Export/ExportOptions.swift` with the three types per the behavior block.

    Implementation notes:
    - `import Foundation` and `import UniformTypeIdentifiers`. No CoreImage, no UIKit, no Photos.
    - `ExportFormat.uti` MUST return the system UTType identifier strings (use `UTType.jpeg.identifier` etc — do NOT hard-code "public.jpeg" so the OS owns the canonical string).
    - `ExportSize` MUST be `Codable` despite the associated value on `.custom` — synthesized Codable handles enums with associated values in Swift 5.5+.
    - `resolve(sourceLongEdge:)` is the single clamp site for the 256...8192 rule. Other plans MUST call this — they MUST NOT re-clamp.
    - Defaults: HEIC + full + 0.85 (per CONTEXT decisions: "Format default: HEIC ... Quality default 0.85").
    - PNG path: `supportsQuality` returns false; downstream UI uses this to hide the quality slider (EXPORT-05 boundary — quality slider is lossy-only).
    - Add a doc comment on `ExportOptions.default` referencing EXPORT-03/04/05.
  </action>
  <verify>
    <automated>grep -E "enum ExportFormat|enum ExportSize|struct ExportOptions|UTType\.(jpeg|heic|png)\.identifier|256\.\.\.8192|case custom\(longEdge" PhotoEditor/Export/ExportOptions.swift | wc -l | awk '$1 >= 6 {exit 0} {exit 1}'</automated>
  </verify>
  <done>
    File exists. All three types declared. UTType identifiers used (not hard-coded strings). Custom-size clamp uses 256...8192 literal. PNG `.supportsQuality` returns false. JSON round-trip preserves `.custom(n)`.
  </done>
</task>

</tasks>

<verification>
- `grep -n "enum ExportFormat" PhotoEditor/Export/ExportOptions.swift` matches.
- `grep -n "enum ExportSize" PhotoEditor/Export/ExportOptions.swift` matches.
- `grep -n "struct ExportOptions" PhotoEditor/Export/ExportOptions.swift` matches.
- `grep -n "256\.\.\.8192\|max(256.*min(8192" PhotoEditor/Export/ExportOptions.swift` matches at least once (clamp present).
- `grep -n "supportsQuality" PhotoEditor/Export/ExportOptions.swift` matches (PNG branch needed by UI plan).
- `grep -n "UTType\.\(jpeg\|heic\|png\)\.identifier" PhotoEditor/Export/ExportOptions.swift` matches three times.
</verification>

<success_criteria>
ExportOptions.swift compiles in isolation (no dependencies on app code). Downstream plans (02–06) can import the file and use `ExportFormat`, `ExportSize`, `ExportOptions` without modification.
</success_criteria>

<output>
After completion, create `.planning/phases/05-export/05-01-SUMMARY.md` describing:
- The three type signatures (copy-paste exact)
- Default values
- Clamp range
- Codable round-trip status
</output>
