---
phase: 06-recipes
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - PhotoEditor/Info.plist
autonomous: true
requirements: [RECIPE-04]
must_haves:
  truths:
    - "iOS recognizes .photorecipe files as belonging to this app (Files app, share sheet, AirDrop)"
    - "Tapping a .photorecipe file in Files launches the app with the URL"
    - "The custom UTI com.photoeditor.recipe is exported and conforms to public.json + public.data"
  artifacts:
    - path: "PhotoEditor/Info.plist"
      provides: "CFBundleDocumentTypes + UTExportedTypeDeclarations entries for .photorecipe / com.photoeditor.recipe"
      contains: "com.photoeditor.recipe"
  key_links:
    - from: "Info.plist CFBundleDocumentTypes"
      to: "com.photoeditor.recipe UTI"
      via: "LSItemContentTypes array"
      pattern: "<string>com.photoeditor.recipe</string>"
    - from: "UTExportedTypeDeclarations"
      to: "public.json + public.data"
      via: "UTTypeConformsTo array"
      pattern: "<string>public.json</string>"
---

<objective>
Register the `.photorecipe` file type with iOS by adding `CFBundleDocumentTypes` (so the system knows this app opens these files) and `UTExportedTypeDeclarations` (so the system knows what `com.photoeditor.recipe` means) to Info.plist.

Purpose: RECIPE-04 sharing requires the system to route `.photorecipe` files to this app — via Files, AirDrop, share-sheet "Open in PhotoEditor", or quick-look preview. Without these plist keys, exported `.photorecipe` files would be treated as opaque blobs.

Output: Updated PhotoEditor/Info.plist with two new top-level keys.
</objective>

<execution_context>
@/home/matt/.claude/get-shit-done/workflows/execute-plan.md
@/home/matt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-recipes/06-CONTEXT.md
@PhotoEditor/Info.plist

<interfaces>
From CONTEXT.md (locked decisions):
- File extension: `.photorecipe`
- Custom UTI: `com.photoeditor.recipe`
- Conforms to: `public.json` and `public.data`

From PhotoEditor/Info.plist current state — does NOT contain:
- CFBundleDocumentTypes (absent)
- UTExportedTypeDeclarations (absent)

Both keys go at the top level of the existing `<dict>`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CFBundleDocumentTypes + UTExportedTypeDeclarations to Info.plist</name>
  <files>PhotoEditor/Info.plist</files>
  <action>
Edit `PhotoEditor/Info.plist`. Insert these two top-level keys (alphabetically near `CFBundleVersion` and the bottom of the dict respectively — final ordering doesn't matter, plist is a dict). Insert the new entries inside the existing `<dict>` element.

**Insert after `CFBundleVersion` block (around current line 20):**

```xml
	<key>CFBundleDocumentTypes</key>
	<array>
		<dict>
			<key>CFBundleTypeName</key>
			<string>Photo Editor Recipe</string>
			<key>CFBundleTypeRole</key>
			<string>Editor</string>
			<key>LSHandlerRank</key>
			<string>Owner</string>
			<key>LSItemContentTypes</key>
			<array>
				<string>com.photoeditor.recipe</string>
			</array>
		</dict>
	</array>
```

**Insert at the end of the dict (just before the closing `</dict>`):**

```xml
	<key>UTExportedTypeDeclarations</key>
	<array>
		<dict>
			<key>UTTypeIdentifier</key>
			<string>com.photoeditor.recipe</string>
			<key>UTTypeDescription</key>
			<string>Photo Editor Recipe</string>
			<key>UTTypeConformsTo</key>
			<array>
				<string>public.json</string>
				<string>public.data</string>
			</array>
			<key>UTTypeTagSpecification</key>
			<dict>
				<key>public.filename-extension</key>
				<array>
					<string>photorecipe</string>
				</array>
				<key>public.mime-type</key>
				<array>
					<string>application/x-photoeditor-recipe</string>
				</array>
			</dict>
		</dict>
	</array>
```

Critical points:
- `LSHandlerRank=Owner` (not Default/Alternate) — this app is the canonical owner of `.photorecipe` files
- `CFBundleTypeRole=Editor` — app can both view and modify recipes
- File extension is `photorecipe` (no leading dot in plist tag spec)
- MIME type is custom — there's no registered MIME for this extension and that's fine
- Do NOT modify any existing keys in the file (NSPhotoLibraryUsageDescription, etc.)

This is a plain XML file — Linux edit is safe (per environment_constraints).
  </action>
  <verify>
    <automated>grep -q "CFBundleDocumentTypes" PhotoEditor/Info.plist &amp;&amp; grep -q "UTExportedTypeDeclarations" PhotoEditor/Info.plist &amp;&amp; grep -q "com.photoeditor.recipe" PhotoEditor/Info.plist &amp;&amp; grep -q "public.json" PhotoEditor/Info.plist &amp;&amp; grep -q "public.data" PhotoEditor/Info.plist &amp;&amp; grep -q "<string>photorecipe</string>" PhotoEditor/Info.plist &amp;&amp; grep -q "LSHandlerRank" PhotoEditor/Info.plist &amp;&amp; xmllint --noout PhotoEditor/Info.plist 2>/dev/null || python3 -c "import plistlib; plistlib.loads(open('PhotoEditor/Info.plist','rb').read())"</automated>
  </verify>
  <done>Info.plist parses as valid XML/plist; both CFBundleDocumentTypes and UTExportedTypeDeclarations entries present and reference com.photoeditor.recipe + photorecipe extension.</done>
</task>

</tasks>

<verification>
- `xmllint --noout` (or `plistlib.loads`) parses Info.plist without error
- Both new keys present at top level
- UTI string is exactly `com.photoeditor.recipe`
- File extension is exactly `photorecipe`
- Conforms-to includes both `public.json` and `public.data`
</verification>

<success_criteria>
- iOS device receiving a .photorecipe via AirDrop offers PhotoEditor as the open-in target
- The .onOpenURL handler (added in plan 06-06) actually fires when a .photorecipe is tapped
- Quick Look in Files shows the file with PhotoEditor's icon (or generic if no icon set)
- (Reminder per Blockers in STATE.md: this MUST be verified on a real device after the full Phase 6 lands)
</success_criteria>

<output>
After completion, create `.planning/phases/06-recipes/06-04-SUMMARY.md`.
</output>
